# AWS CLI Configuration
# https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-files.html

[default]
region = us-east-1
output = json
cli_pager =
cli_history = enabled
s3 =
    max_concurrent_requests = 10
    max_queue_size = 1000
    multipart_threshold = 64MB
    multipart_chunksize = 16MB
    addressing_style = auto
parameter_validation = true
tcp_keepalive = true
max_attempts = 3
retry_mode = adaptive

# Common profiles
[profile development]
region = us-east-1
output = json
# source_profile = default
# role_arn = arn:aws:iam::ACCOUNT:role/DeveloperRole

[profile staging]
region = us-east-1
output = json
# source_profile = default
# role_arn = arn:aws:iam::ACCOUNT:role/StagingRole

[profile production]
region = us-east-1
output = json
# source_profile = default
# role_arn = arn:aws:iam::ACCOUNT:role/ProductionReadOnlyRole
# mfa_serial = arn:aws:iam::ACCOUNT:mfa/USERNAME

# Regional profiles
[profile eu-west-1]
region = eu-west-1
output = json

[profile ap-southeast-1]
region = ap-southeast-1
output = json
